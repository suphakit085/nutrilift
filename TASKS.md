# รายการงานและความคืบหน้า

ติดตามงานตามแผนที่อนุมัติไว้ อัปเดตล่าสุด **31 ส.ค. 2569**

สัญลักษณ์
`[x]` เสร็จและตรวจแล้ว · `[~]` โค้ดเสร็จแต่ยังไม่พิสูจน์ / ยังไม่ครบเป้า · `[ ]` ยังไม่เริ่ม

---

## ภาพรวม

| ด้าน | ความคืบหน้า |
|---|---|
| ระบบ (โค้ด) | ▓▓▓▓▓▓▓▓▓▓ ~95% (เหลือ deploy) |
| ฐานความรู้ | ▓▓▓▓░░░░░░ 12 / 25–35 การ์ด |
| ฐานข้อมูลอาหาร | ▓▓▓▓▓▓▓▓▓░ **339 แถว · ข้อมูลจริง 312** (เกินเป้า 150–300) |
| ชุดคำถามทดสอบ | ▓▓▓▓░░░░░░ 33 / 80–100 |
| การประเมินผล | ▓▓▓▓▓░░░░░ อัตโนมัติเสร็จ · ผู้เชี่ยวชาญ/SUS ยังไม่เริ่ม |
| Deploy | ░░░░░░░░░░ ยังไม่เริ่ม |
| เขียนเล่ม | ░░░░░░░░░░ ยังไม่เริ่ม |

**สรุปสั้น ๆ:** โค้ดล้ำหน้าแผนไปมาก (แผนวางไว้สัปดาห์ 5–8 แต่เสร็จตั้งแต่สัปดาห์ 1)
งานที่เหลือส่วนใหญ่**ไม่ใช่การเขียนโค้ด** แต่เป็นการเขียนเนื้อหาและเก็บข้อมูล

---

## MUST — ต้องมีจึงจะสอบผ่าน

- [x] **1. บัญชีผู้ใช้** — สมัคร/ล็อกอิน JWT, `passlib[bcrypt]` + `python-jose`
  ทดสอบผ่าน API จริงแล้ว: register → login → /auth/me → 401 เมื่อไม่มีโทเคน
- [x] **2. โปรไฟล์** — เพศ อายุ ส่วนสูง น้ำหนัก %ไขมัน ระดับกิจกรรม วันเล่นเวท เป้าหมาย ข้อจำกัดอาหาร
  ครบทุกฟิลด์ตามแผน · ปฏิเสธค่าที่สูตรคำนวณไม่รองรับ (422) · ภาษาไทยเก็บลง DB ถูกต้อง
- [x] **3. เครื่องคำนวณ deterministic** — Mifflin-St Jeor / Katch-McArdle / TDEE / มาโคร
  21 unit tests · ตรวจค่ากับการคำนวณมือแล้ว (ชาย 25 ปี 175/70 moderate → BMR 1,674 TDEE 2,594)
  พร้อมคำเตือนและรายการอ้างอิงในผลลัพธ์
- [~] **4. RAG Q&A ภาษาไทย** — โค้ดเสร็จครบ แต่ฐานความรู้ยังบางมาก
  - [x] embed + ค้นด้วย pgvector + แสดง `[S1]` citation
  - [x] กรองแหล่งอ้างอิงให้เหลือเฉพาะที่คำตอบใช้จริง
  - [x] กรองสองชั้น (ประตูขอบเขต + หน้าต่างสัมพัทธ์)
  - [~] **เขียนการ์ดความรู้ (8 / เป้าใหม่ 25–35)** — protein, energy, creatine, timing,
        caffeine, sodium-water, fiber, calcium-iron-vitd · สามใบล่าสุดอิง Thai DRI 2563
        ⚠️ ค่าธาตุเหล็กชายอยู่ในตารางที่เป็นภาพ (ตารางที่ 6 หน้า 303) ต้องเปิดดูด้วยตาแล้วเติม
- [~] **5. ฐานข้อมูลอาหารไทย** — ข้อมูลจริงแล้ว 308 จาก 339 แถว
  - [x] ตาราง `foods` + `lookup_food` tool + ingest จาก CSV
  - [x] **ดึงข้อมูลจริงจาก ASEAN Food Composition Database (INMU) ได้ 308 รายการ**
        ผ่านด่านตรวจพลังงาน-สารอาหาร และตรวจกับค่าที่ทราบอิสระแล้ว
  - [x] เกินเป้า 150–300 รายการที่วางไว้ในแผน
  - [x] จับคู่รายการวัตถุดิบเดี่ยวที่เหลือกับ ASEAN อีกรอบ (ตรวจชื่ออังกฤษคู่ไทยเสมอ) → ได้เพิ่ม 4 รายการ
  - [ ] **เหลือ 25 แถวที่ยังเป็นค่าชั่วคราว** — ทั้งหมดเป็นเมนูจานเดียว/ปรุงสำเร็จ (ข้าวมันไก่ ผัดไทย
        ส้มตำ ต้มยำ ลาบ ฯลฯ) ที่ ASEAN ไม่มีเพราะเป็นฐานวัตถุดิบเดี่ยว ต้องหาแหล่งที่ให้ค่าต่อจาน
        (Thai FCD ของ INMU น่าจะมี แต่ต้องค้นทีละเมนู หรือรอ OCR ตารางกรมอนามัย)
  - [x] เวย์โปรตีน + ชานมไข่มุก ยืนยันแล้วว่าถูกต้องที่เป็น `TOVERIFY-LABEL` (ไม่ใช่ของที่ต้องแก้)
- [~] **6. Chat UI** — client ทดสอบกับ backend จริงแล้ว เหลือแค่ตรวจหน้าตาด้วยสายตา
  - [x] streaming SSE, ประวัติการสนทนา, แผงแหล่งอ้างอิง, หน้าโปรไฟล์แสดงเป้าหมาย
  - [x] build + type-check + eslint ผ่าน
  - [x] **SSE parser: แก้บั๊ก CRLF + unit test 14 ข้อกับรูปแบบจริงจากเซิร์ฟเวอร์**
  - [x] ยิง `streamChat()` จริงไปที่ backend: ได้ 4 sources, 154 delta, done, บันทึกลง DB ครบ
  - [ ] เปิดในเบราว์เซอร์เพื่อดูการจัดวาง/สไตล์ (ต้องใช้สายตาคน — ตรรกะพิสูจน์แล้ว)
- [x] **7. Guardrails** — กฎแบบ rule-based (ไม่พึ่ง LLM) 6 หมวด, 24 tests
  disclaimer · ปฏิเสธนอกขอบเขต · ไม่ให้ขนาดยา/สเตียรอยด์ · ส่งต่อผู้เชี่ยวชาญ
  ทดสอบกับ LLM จริงแล้ว: ปฏิเสธได้ 100% ในหมวดความปลอดภัย
- [x] **8. Evaluation harness** — `eval/run_eval.py` รันจริงแล้ว 2 รอบ
  RAG vs no-RAG · retrieval metrics · LLM-as-judge (คนละโมเดลกับ generator) · ไฟล์ blinded ให้ผู้เชี่ยวชาญ

---

## การประเมินผล (ตามแผนข้อ 4)

- [~] **ชุดคำถามทดสอบ** — มี 15 ข้อ เป้า 80–100 ข้อ
  - [x] โครงสร้างไฟล์ + เฉลย + `relevant_doc_slugs` ครบทุกข้อ
  - [ ] **ขยายเป็น 80–100 ข้อ**
  - [ ] ให้อาจารย์/นักกำหนดอาหารรับรองเฉลย
- [x] **Retrieval metrics** — hit@k = 1.00, MRR = 0.938
- [x] **คุณภาพคำตอบอัตโนมัติ** — correctness / completeness / groundedness / hallucination
- [x] **เปรียบเทียบ RAG vs no-RAG** — RAG ชนะทุกตัวชี้วัด ไม่มีข้อไหนแย่ลง
- [ ] **ผู้เชี่ยวชาญให้คะแนน blind** — ระบบสร้างไฟล์ `*_expert.csv` ให้แล้ว รอส่งให้ผู้เชี่ยวชาญ
- [x] **สืบค้นงานที่เกี่ยวข้องสำหรับบทที่ 2** → [`docs/related-work.md`](docs/related-work.md)
- [ ] **SUS + ความพึงพอใจจากผู้ใช้จริง 20–30 คน** — ต้อง deploy ก่อน

### ผลล่าสุด (final-v7 — 12 การ์ด, 33 คำถาม, ฐานอาหารจริง 308 รายการ)

| ตัวชี้วัด | RAG | ไม่ใช้ RAG |
|---|---|---|
| correctness | **4.85** | 4.52 |
| groundedness | **4.39** | 3.12 |
| hit@k | **0.967** | — |
| hallucination | 0% | 0% |
| ปฏิเสธคำถามเสี่ยงได้เหมาะสม | 100% | 100% |

⚠️ n=33 ยังไม่พอทำ Wilcoxon ให้มีอำนาจทางสถิติ ต้องรันซ้ำเมื่อคำถามครบ 80–100

---

## SHOULD — ทำเฉพาะเมื่อ MUST ครบและ deploy แล้ว

> กฎคุมขอบเขตจากแผน: **ห้ามเริ่มข้อใดข้อหนึ่งจนกว่า MUST จะครบและ deploy แล้ว**

- [x] ปุ่มคำถามแนะนำ (quick-reply) — ทำไปแล้วระหว่างสร้างหน้าแชต
- [ ] แนะนำเมนู 1 วันให้ตรงมาโคร
- [x] **Hybrid search (dense + BM25 + RRF)** — ทำแล้วเมื่อ hit@k ลงถึง 0.833 ตามเงื่อนไขที่วางไว้
      ผล: hit@k 0.833 → **0.967**, correctness 4.61 → **4.82** (17 เทสต์)
- [ ] หน้า admin อัปโหลด/re-index — แผนอนุญาตให้ใช้ CLI แทนได้ ซึ่งทำแล้ว

---

## งานที่โผล่มาเพิ่มจากการทดสอบ (ไม่มีในแผนเดิม)

- [x] แยกการกรอง retrieval เป็นสองชั้น (เจอตอน eval ว่า threshold เดียวตัด chunk สำคัญทิ้ง)
- [x] กันโมเดลแต่งค่าอินพุตของเครื่องมือ (เจอว่ามันแต่ง `body_fat_pct` เอง)
- [x] `_clean_overrides()` ตัดค่า placeholder ที่โมเดลใส่มา
- [x] แก้ `.env` ให้อ่านจาก path จริง ไม่อิงตำแหน่งที่รันคำสั่ง
- [x] ย้าย ruff config ไป root (เดิม `eval/` หลุดการตรวจ)
- [x] **แก้ SSE parser ที่ตัดเฟรมด้วย LF ทั้งที่เซิร์ฟเวอร์ส่ง CRLF** (หน้าแชตจะไม่ขึ้นข้อความเลย)
- [x] เพิ่ม `npm test` ฝั่ง frontend (Node test runner ไม่ต้องลง dependency เพิ่ม)
- [x] **rate limiting** — ต่อผู้ใช้ (20/ชม., 60/วัน) + เพดานรวม 1,500/วัน + ล็อกอิน 10/15 นาที ต่อ IP
      19 เทสต์ · ยืนยันจริงว่าบล็อกก่อนเรียก LLM และไม่เขียนลง DB
- [x] ปรับชื่อในโค้ดเป็น **NutriLift** (คง 'โค้ชนัท' ไว้เป็นชื่อผู้ช่วยใน system prompt)
- [x] ปุ่มลบห้องแชตใน UI (ลบแบบ optimistic คืนค่าถ้าลบไม่สำเร็จ ไม่ใช้ window.confirm ที่บล็อกหน้า)
- [x] **ย้าย LLM provider จาก OpenAI ไป Gemini free tier ทั้งระบบ (31 ส.ค. 2569)** — เครดิต OpenAI
      หมด ผู้ใช้เลือก Gemini free tier แทนการเติมเงิน แก้ `llm.py`/`chat.py`/`eval/run_eval.py`,
      re-embed การ์ดทั้ง 12 ใบ (เวกเตอร์ OpenAI เทียบกับ Gemini ไม่ได้แม้มิติเท่ากัน), recalibrate
      `RETRIEVAL_MIN_SCORE` 0.32→0.63 (สเกล cosine similarity เปลี่ยนไปตามโมเดล embedding), smoke test
      ผ่านครบ (RAG+citations, tool calling ทั้งแบบ precomputed และ override, ปฏิเสธนอกขอบเขต, safety flag)
      ⚠️ **พบเพดาน 20 requests/วันของ `gemini-3.5-flash` จริงระหว่างทดสอบ** (error 429) และ
      `gemini-2.5-flash`/`-lite` ถูกเลิกใช้กับโปรเจกต์นี้แล้ว (404) จึงเปลี่ยน `JUDGE_MODEL` เป็น
      `gemini-3.6-flash` — รายละเอียดและผลกระทบต่อแผน eval/SUS ดู
      [`docs/architecture.md`](docs/architecture.md) หัวข้อ "Gemini free tier"
- [ ] **รัน eval harness ใหม่ด้วย Gemini** — ค้างเพราะโควตา `gemini-3.5-flash` (20/วัน) หมดจาก
      smoke test ตอนย้ายระบบไปแล้ว ต้องรอโควตารีเซ็ต แล้วบันทึกผลเป็น baseline-v3 แทนที่ตัวเลข
      OpenAI-era ในหัวข้อ 8 ของ `docs/architecture.md` (ดูหมายเหตุที่ใส่ไว้ตรงนั้น)

---

## ไทม์ไลน์ 16 สัปดาห์

| สัปดาห์ | งานตามแผน | สถานะ |
|---|---|---|
| 1–2 | Proposal, ขอบเขต, ตั้ง repo/docker/DB, เริ่มรวบรวมเอกสาร | 🟡 repo+DB เสร็จ · **มีลิงก์ดาวน์โหลดเอกสารครบแล้วใน SOURCES.md** · ยังไม่ได้โหลดไฟล์จริง · proposal ยังค้าง |
| 3–4 | เขียน knowledge cards 60+, foods.csv, ingest, ทดสอบ retrieval | 🟡 ingest + ทดสอบ retrieval เสร็จ · **การ์ด 3/60 · foods ยังเป็นค่าชั่วคราว** |
| 5–6 | Backend: auth, profile, calculators, orchestrator, SSE | 🟢 **เสร็จแล้ว (ล่วงหน้า)** |
| 7–8 | Frontend: login/profile/chat + เชื่อม backend | 🟢 เสร็จแล้ว (ล่วงหน้า) · เหลือทดสอบในเบราว์เซอร์ |
| 9 | Guardrails, citations, polish, deploy | 🟡 guardrails + citations เสร็จ · **deploy ยังไม่เริ่ม** |
| 10–11 | ชุดคำถาม + เฉลย, รัน eval, ปรับ retrieval/prompt | 🟡 harness + รันจริงเสร็จ (ล่วงหน้า) · **ชุดคำถาม 15/80** |
| 12–13 | ผู้เชี่ยวชาญให้คะแนน + SUS 20–30 คน | 🔴 ยังไม่เริ่ม |
| 14–16 | เขียนเล่ม, สไลด์, ซ้อมสอบ | 🔴 ยังไม่เริ่ม |

---

## ลำดับที่ควรทำต่อ

1. **ดาวน์โหลดเอกสารตามลิงก์ใน `knowledge/SOURCES.md` → เขียนการ์ดความรู้** ← คอขวดตัวจริง
2. เปิดหน้าเว็บดูด้วยตาสักรอบ (`npm run dev` → localhost:3000) ตรวจการจัดวาง
3. **แทนข้อมูล `foods.csv` ด้วยของจริง**
4. รอโควตา `gemini-3.5-flash` รีเซ็ต → รัน eval เต็มชุดด้วย Gemini (baseline-v3) → ขยายชุดคำถามเป็น
   80–100 (`RETRIEVAL_MIN_SCORE` recalibrate ไปที่ 0.63 แล้ว ไม่ต้องรัน `calibrate_threshold.py` ซ้ำ
   จนกว่าจะเปลี่ยนโมเดล embedding อีก หรือฐานความรู้โตขึ้นมาก)
5. deploy → เก็บ SUS + ให้ผู้เชี่ยวชาญตรวจ (rate limiting พร้อมแล้ว) — ถ้าเพดาน 20 req/วันของ
   `gemini-3.5-flash` เป็นคอขวดตอนเก็บ SUS จริง (20–30 คนพร้อมกัน) ให้พิจารณา paid tier ช่วงสั้น ๆ
   เฉพาะช่วงเก็บข้อมูล
6. เขียนเล่ม (ใช้ `docs/architecture.md` เป็นต้นฉบับบทที่ 3)

**เอกสารอ้างอิง:** สถาปัตยกรรม [`docs/architecture.md`](docs/architecture.md) ·
ฐานความรู้ [`knowledge/README.md`](knowledge/README.md) ·
เช็กลิสต์เอกสาร [`knowledge/SOURCES.md`](knowledge/SOURCES.md)
